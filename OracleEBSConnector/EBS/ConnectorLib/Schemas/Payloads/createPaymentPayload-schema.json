{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "title": "Create Payment Payload JSON Schema",

  "type": "object",
  "properties": {
    "companyName": { "type": "string" }, // Defines EBS Company Name to select
    "payment": { "$ref": "payment-schema.json" } 
  },
  "required": [
    "companyName",
    "payment"
  ]
}